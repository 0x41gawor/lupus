---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: decides.lupus.gawor.io
spec:
  group: lupus.gawor.io
  names:
    kind: Decide
    listKind: DecideList
    plural: decides
    singular: decide
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Decide is the Schema for the decides API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DecideSpec defines the desired state of Decide
            properties:
              name:
                description: |-
                  INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                  Name is a string field
                type: string
              nextElement:
                description: NextElement
                type: string
              url:
                description: Url contains URL details with path and method of Open
                  Policy Agent endpoint to be hit by
                properties:
                  method:
                    type: string
                  path:
                    type: string
                required:
                - method
                - path
                type: object
            required:
            - name
            - nextElement
            - url
            type: object
          status:
            description: DecideStatus defines the observed state of Decide
            properties:
              input:
                description: Input for the Decision
                type: object
                x-kubernetes-preserve-unknown-fields: true
              lastUpdated:
                description: Timestamp of the last update
                format: date-time
                type: string
            required:
            - input
            - lastUpdated
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
