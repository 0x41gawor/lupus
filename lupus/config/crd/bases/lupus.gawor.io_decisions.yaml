---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: decisions.lupus.gawor.io
spec:
  group: lupus.gawor.io
  names:
    kind: Decision
    listKind: DecisionList
    plural: decisions
    singular: decision
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Decision is the Schema for the decisions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DecisionSpec defines the desired state of Decision
            properties:
              foo:
                description: Foo is an example field of Decision. Edit decision_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: DecisionStatus defines the observed state of Decision
            properties:
              decision:
                items:
                  description: MoveCommand represents a move command with source,
                    destination, and count
                  properties:
                    count:
                      type: integer
                    from:
                      type: string
                    to:
                      type: string
                  required:
                  - count
                  - from
                  - to
                  type: object
                type: array
              input:
                description: Input defines the 4 integer fields for different locations
                properties:
                  gdansk:
                    type: integer
                  krakow:
                    type: integer
                  poznan:
                    type: integer
                  warsaw:
                    type: integer
                required:
                - gdansk
                - krakow
                - poznan
                - warsaw
                type: object
            required:
            - input
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
